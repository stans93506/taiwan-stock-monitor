# 台股監測專案 - 工作進度

## 目前正在做的任務

季報 tab 缺少某些公司的財報公告（4562 穎漢、1454 台富、2501 國建）

---

## 已修改的檔案

### `fetch_revenue.py`
1. **更新時間改顯示台灣時間**
   - `datetime.now()` → `datetime.utcnow() + timedelta(hours=8)`

2. **所有表格取消分頁**
   - `#qtrTable`、`#trsTable`、`#eventTable`、`#spoTable` 的 `pageLength: 50` → `paging: false`

3. **現增排除非新決議公告關鍵字**（`SPO_EXCLUDE` 新增）
   - `放棄認購`、`股款催繳`、`通過認購`、`催繳期間屆滿`、`股票發放`、`調整現金增資`

4. **季報過濾條件修正（三處）**
   - 移除 `QTR_EXCLUDE` 中的 `"年度合併財務"`（避免誤排「上半年度合併財務」如 2501 國建）
   - 在季報判斷條件（共三處：第 5175、5332、5450 行）加入 `"第二" in r["desc"]`，
     使「第二合併財務報告」（4562 穎漢）、「第二財務報告」（1454 台富）能被正確抓取

### `.github/workflows/daily.yml`
- 排程改為 `cron: '30 */2 * * *'`（每兩小時共 12 次）
- `git push` 前加入 `git pull --rebase origin main || true` 防衝突

### `spo_cache.json`
- 手動刪除 3149、7823、3540、6492 四筆誤入的後續公告

---

## 尚未完成的事項

（無——等待 4562/1454 申報後自動觸發）

---

## 測試結果

| 項目 | 結果 |
|------|------|
| 現增誤入公告（3149/7823/3540/6492）| ✅ 已排除 |
| 2501 國建上半年度合併財務報告 | ✅ 已在 qtr_cache（08/05 17:03） |
| 4562 穎漢 / 1454 台富「第二財務報告」| ⏳ 尚未申報（期限 8/31），filter 已修正等待觸發 |
| GitHub Actions YAML 語法錯誤 | ✅ 已修復 |
| 排程每兩小時 `*/2` 語法 | ✅ 已推送 |
| fetch_revenue.py 推送至 origin/main | ✅ commit bf15ec3 |

### Filter 驗證（2026-08-05）
- `"第二" in desc` 已加入三處 filter（5175、5332、5450 行）
- `QTR_EXCLUDE` 用 `"年度財務"` 而非 `"年度合併財務"`，不會誤排「上半年度合併財務報告」
- 模擬測試：4562/1454/2501 三支股票的標題格式均可正確通過 filter
